import React from 'react'
import { Link } from 'react-router'

export const Template = () => (
  <div>
    contents
  </div>
)

Template.propTypes = {
  prop : React.PropTypes.element.isRequired
}

export default Template
